export interface Subscription { id: string, params: any }

export interface SubscriptionCreation {
  id: string,
  email: string,
  updatedAt: string,
  subscription_active: boolean,
  createdAt: string,
}

